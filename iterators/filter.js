// using filter we can retrun the single values of the array whereas in for each loop we can't work on single values

const num = [1,23,3,211,434,3454,532,]
// without parenthsis
// const newnum = num.filter( (num) => num > 211)

// with parenthsis
// const newnum = num.filter( (num) => (num > 211))

// with scope
// const newnum = num.filter( (num) => 
    // {
    //     num > 211)
    // }

// console.log(newnum)



// foreach loop id we want to return the value


// const nnum = []
// num.forEach ( (num) =>{
//     if (num >211) {
//         nnum.push(num);
//     }
// }
// )

// console.log(nnum);

